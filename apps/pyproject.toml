[project]
name = "smarthome"
version = "0.1.0"
description = "Зависимости для разработки проекта: PlantUML и тд."
authors = [
    {name = "protasovse",email = "protasovse@yandex.ru"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.13,<4"
dependencies = [
    "plantuml (>=0.3.0,<0.4.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
